<!-- Generated by Tses 2026-07-09T12:37:38Z -->

# TSES Django Scaffold

A lean Django foundation for building API-first services with Django REST Framework, Simple JWT, PostgreSQL, Celery workers, Celery Beat scheduling, and scaffold-friendly helper classes for pagination, error formatting, users, notifications, and activity logging.

## What Ships in the Scaffold

- Django project bootstrapped under `config`
- Django REST Framework configured as the API layer
- Simple JWT configured for bearer-token authentication
- PostgreSQL database configuration
- Custom email-first user model
- Notification model for in-app notification persistence
- User activity log model for reusable audit trails
- Standardized API error formatting
- Standardized paginated response helpers
- Celery worker and scheduler services in Docker Compose
- Docker setup for an `api`, `worker`, and `scheduler` service on `tses_network`
- GitHub Actions workflow for building and pushing Docker images to DigitalOcean Container Registry

## Project Layout

```text
django-scaffold/
├── .docker/
│   ├── Dockerfile
│   └── entrypoint.sh
├── .github/
│   └── workflows/
│       └── deploy.yaml
├── api/
│   ├── authentication/
│   │   └── utils/
│   │       └── base_auth.py
│   ├── authorization/
│   ├── notification/
│   │   ├── enums.py
│   │   ├── models.py
│   │   ├── serializers.py
│   │   └── views.py
│   └── users/
│       ├── enums.py
│       ├── migrations/
│       ├── models/
│       │   ├── activity_log.py
│       │   ├── manager.py
│       │   └── user.py
│       ├── serializers/
│       └── views/
├── config/
│   ├── celery.py
│   ├── settings/
│   │   ├── app_registry.py
│   │   ├── celery.py
│   │   ├── commons.py
│   │   ├── cors.py
│   │   ├── db.py
│   │   ├── drf.py
│   │   ├── exception.py
│   │   ├── jwt.py
│   │   ├── middleware.py
│   │   ├── s3.py
│   │   └── smtp.py
│   ├── urls.py
│   └── wsgi.py
├── includes/
│   ├── helpers/
│   │   ├── formatter.py
│   │   ├── models.py
│   │   └── pagination.py
│   └── third_party/
├── .env.example
├── docker-compose.yaml
├── manage.py
└── requirements.txt
```

## Requirements

- Python 3.12+
- PostgreSQL
- Redis
- Docker and Docker Compose for containerized development
- An external Docker network named `tses_network` when using Compose

PostgreSQL and Redis are expected to come from your shared infrastructure repository and be reachable on `tses_network`.

## Quick Start

### Local Development

1. Create an environment file:

```bash
cp .env.example .env
```

2. Update at least the database, secret key, and JWT settings.

3. Install dependencies:

```bash
pip install -r requirements.txt
```

4. Apply migrations:

```bash
python manage.py migrate
```

5. Start Django:

```bash
python manage.py runserver 0.0.0.0:8000
```

6. Start Celery services in separate terminals when needed:

```bash
celery -A config worker --loglevel=info
celery -A config beat --loglevel=info --schedule /tmp/celerybeat-schedule
```

### Docker Development

The Compose file starts three application services:

- `api`: Django development server
- `worker`: Celery worker
- `scheduler`: Celery Beat scheduler

Start the scaffold with Docker:

```bash
docker compose up --build
```

Notes:

- `api` exposes port `8000`
- the shared entrypoint runs `manage.py check`
- `api` runs migrations when `DJANGO_RUN_MIGRATIONS=1`
- `api` can run `collectstatic` when `DJANGO_COLLECTSTATIC=1`
- `worker` and `scheduler` explicitly disable migrations and static collection
- all services join the external Docker network `tses_network`

## Environment Variables

Copy `.env.example` and adjust the values for your project.

### Core Django Settings

- `DJANGO_SETTINGS_MODULE`: Django settings module path
- `SECRET_KEY`: Django secret key
- `DEBUG`: enables debug mode
- `ALLOWED_HOSTS`: comma-separated allowed hosts list

### Database Settings

- `DB_HOST`
- `DB_PORT`
- `DB_NAME`
- `DB_USER`
- `DB_PASSWORD`

### JWT Settings

- `SIGNING_KEY`: signing key used by Simple JWT

### Container Startup Settings

- `DJANGO_RUN_MIGRATIONS`: run migrations automatically in the `api` container when set to `1`
- `DJANGO_COLLECTSTATIC`: run `collectstatic` automatically in the `api` container when set to `1`

### Celery Settings

- `CELERY_BROKER_URL`: Redis broker URL
- `CELERY_RESULT_BACKEND`: Celery result backend URL

### Email Settings

- `EMAIL_HOST`
- `EMAIL_PORT`
- `EMAIL_USE_SSL`
- `EMAIL_USE_TLS`
- `EMAIL_HOST_USER`
- `EMAIL_HOST_PASSWORD`
- `DEFAULT_FROM_EMAIL`

In debug mode, email defaults to Django’s console backend. SMTP settings become relevant when debug is off.

## 🧭 Development Workflow & Contribution Guidelines

To keep the codebase clean, predictable, and easy to review, all development should follow the workflow below.

---

### 🌱 Branching Strategy

Before starting any new work, **always create a new branch** from `main`.

### Branch naming convention

```text
feature/<short-description>
fix/<short-description>
chore/<short-description>
```

### Examples

```bash
feature/user-authentication
fix/payment-webhook-bug
chore/update-dependencies
```

> ❌ Do not commit directly to `main`

---

### 📝 Commit Style (Git Standard)

We follow **Conventional Commits** — clear, readable, and automation-friendly.

### Commit format

```text
<type>: <short summary>
```

### Allowed types

- `feat` – new feature
- `fix` – bug fix
- `chore` – tooling, config, non-functional changes
- `docs` – documentation only
- `refactor` – code changes without new features or fixes
- `test` – adding or updating tests

### Examples

```bash
feat: add user registration endpoint
fix: prevent duplicate bookings
refactor: simplify payment service logic
docs: update API documentation
```

### 🪝 Commit Message Hook (Local Setup)

This project enforces **Conventional Commit** style using a Git `commit-msg` hook.

> ⚠️ Note: Git hooks are **local-only** by default.
> They are **not committed to the repository**, and **each developer must set them up locally**.
> This is normal Git behavior.

---

## 🔍 Pull Request (PR) Guidelines

Every PR **must** include the following sections:

### 1️⃣ What has changed?

A clear, concise summary of what this PR does.

Example:

> Added user login and JWT authentication endpoints.

---

### 2️⃣ Where were the changes done?

List the main areas/files affected.

Example:

- `core/api/views/auth.py`
- `core/api/serializers/auth.py`
- `feexeet/settings/base.py`

---

### 3️⃣ What should the reviewer know?

Important context for the reviewer:

- Edge cases
- Breaking changes
- Things to pay extra attention to
- Follow-up work (if any)

Example:

> Token expiry is set to 15 minutes. Refresh token rotation will be added in a follow-up PR.

---

## ✅ PR Checklist (before requesting review)

- [ ] Code follows PEP 8
- [ ] Commits follow Conventional Commit format
- [ ] No secrets committed
- [ ] API changes documented (Swagger updated)
- [ ] Tests added or updated (where applicable)

---

## Docker Compose Services

### `api`

Runs the Django development server and handles startup checks and migrations.

### `worker`

Runs the Celery worker:

```bash
celery -A config worker --loglevel=info
```

Use this service for asynchronous tasks such as background emails, notification fan-out, report generation, or other non-blocking jobs.

### `scheduler`

Runs Celery Beat:

```bash
celery -A config beat --loglevel=info --schedule /tmp/celerybeat-schedule
```

Use this service for scheduled or recurring tasks.

## Default Routes

The scaffold currently registers only one built-in route:

- `GET /admin/`

Django REST Framework, drf-spectacular, and the scaffold app modules are installed, but API endpoints and schema routes are not yet wired into `config/urls.py`. That is intentional: generated projects can expose only the routes they actually need.

## Authentication and JWT

The scaffold uses:

- `rest_framework_simplejwt.authentication.JWTAuthentication`
- bearer tokens in the `Authorization` header
- an email-first custom user model

Simple JWT is configured in `config/settings/jwt.py` with:

- longer access-token lifetime in debug mode
- refresh-token rotation enabled
- token blacklisting enabled

### Authentication Extension Point

The scaffold includes an abstract authentication contract in `api/authentication/utils/base_auth.py`:

- `signup()`
- `verify_otp()`
- `generate_access_token()`
- `resend_otp()`
- `reset_password()`

Projects can implement OTP, passwordless, social login, or vendor-backed authentication flows against this interface without rewriting the higher-level calling pattern.

### Roles

`UserRole` splits into three groups, and the distinction is load-bearing:

| Group          | Roles                                                | How you get it            |
| -------------- | ---------------------------------------------------- | ------------------------- |
| **Public**     | `COURSE_CREATOR`                                     | Public signup             |
| **Staff**      | `STAFF_WRITER`, `STAFF_VERIFIER`, `STAFF_APPROVER`   | Super Admin invitation    |
| **Privileged** | `ADMIN`, `SUPER_ADMIN`                               | Bootstrap / direct assign |

The three staff roles are the Writer / Verifier / Approver options in the
"Invite a staff" dialog and mirror the authoring pipeline (write → verify →
approve). They are kept distinct from `COURSE_CREATOR` on purpose: a
self-registered member of the public and a hired writer do the same job but are
not the same thing, and collapsing them would make staff unidentifiable in
permissions, reporting, and the Teams page.

Permission classes pair each legacy role with its staff equivalent, so
endpoints do not need to enumerate both: `IsCourseCreatorRole` covers Writers,
`IsCreatorReviewerRole` covers Verifiers, and `IsAdminRole` covers Approvers
and Super Admins.

### Staff Accounts

Staff accounts are never self-service. Public signup always creates a
`COURSE_CREATOR` regardless of what the client posts, so the only route to a
staff or privileged role is the chain below (implemented in
`api/authentication/services/staff_service.py`):

1. **Bootstrap the Super Admin** — `POST /api/v1/auth/superadmin/bootstrap/`

   Claims the platform's single `SUPER_ADMIN` seat. There is no logged-in user
   to authorize this yet, so it is gated on a shared secret instead: the
   request's `bootstrap_token` must match the `SUPERADMIN_BOOTSTRAP_TOKEN`
   environment variable.

   `SUPERADMIN_BOOTSTRAP_TOKEN` defaults to empty, and empty **disables** the
   endpoint (403) rather than accepting an empty token — a deployment that
   forgets the variable is not claimable by whoever finds the URL first. Set it
   to a long random value (`openssl rand -hex 32`), bootstrap once, then clear
   it so the secret stops being a live credential.

   Exactly one `SUPER_ADMIN` can exist. That is enforced by a partial unique
   index (`unique_super_admin` on `api.users.models.User`), not just an
   application check, so two concurrent bootstrap requests cannot both win.
   Replacing the Super Admin is therefore a deliberate operation — demote the
   incumbent first — and not something the API will do for you.

2. **Invite a staff member** — `POST /api/v1/auth/staff/invitations/`

   Super Admin only; Approvers and Admins cannot invite, which is the
   capability that sets the Super Admin apart. `role` accepts only the three
   invitable staff roles — `SUPER_ADMIN` is rejected, so this endpoint cannot
   mint a second platform owner. Creates the invitee inactive with an unusable
   password (reserving the email) and emails a single-use link. Re-inviting
   someone still pending reissues the link, invalidates the previous one, and
   updates their role, which is how a mis-selected role gets corrected before
   acceptance.

3. **Accept the invitation** — `POST /api/v1/auth/staff/invitations/accept/`

   The invitee sets their own password and the account activates in the
   invited role, returning a JWT pair. Super Admins therefore never know
   another operator's credentials, and the role cannot be influenced by the
   invitee.

The Teams page is backed by three more Super Admin endpoints:

- `GET /api/v1/auth/staff/` — the roster, including pending and revoked rows.
- `POST /api/v1/auth/staff/{id}/revoke/` — withdraw a pending invitation
  (burning its token) or deactivate an active member.
- `POST /api/v1/auth/staff/{id}/reactivate/` — restore a revoked member.

Read `invitation_status` rather than `is_active` when rendering a row: `PENDING`
and `REVOKED` are both inactive but offer different actions, and they are told
apart by whether an unconsumed invitation token still exists. An invitation
revoked *before* acceptance cannot be reactivated — that account has no usable
password — so those must be re-invited instead.

Neither revoke nor reactivate can target your own account, and the Super Admin
seat cannot be revoked at all: it is bootstrap-only, so a revoked Super Admin
could not be replaced through the API.

Django's `is_staff` / `is_superuser` flags are set on the bootstrapped Super
Admin so it can also reach `/admin/`. Note that `is_superuser` bypasses every
role check in `api.users.permissions.HasRole`, so a Django superuser created
via `createsuperuser` passes as any role — that is Django's convention, but it
means shell-created superusers sidestep the invitation chain above.

### Category Access

Categories live in their own app (`api/categories/`) with the standard
model / serializers / services / views layout. Course and CreatorProfile both
hold foreign keys into it, so `categories` is a dependency of `courses` and
`onboarding` — never the reverse.

Write access is narrower than everywhere else in the platform and does not
follow the usual "admins can do anything" shape:

| Action                | Who                       |
| --------------------- | ------------------------- |
| List / retrieve       | Any authenticated user    |
| Create/update/delete  | Writer, Super Admin       |

Admins and Approvers deliberately get **read only** here (`CanManageCategories`
in `api.users.permissions`), even though they have full course CRUD. Note also
that this permission is not `IsCourseCreatorRole`, which would let public
Course Creators in — category management is a staff job, and public creators
only browse categories to pick one.

Two things worth knowing before changing a category:

- **`creator_price` is a payout lever, and Writers can set it.** A Writer can
  raise a category's price and then submit their own course against it. That is
  an accepted trust decision for salaried staff, not an oversight. Price changes
  are not retroactive — `Course.creator_price_snapshot` freezes the rate at
  submission time.
- **Deleting is restricted by the database, not just by convention.**
  `Course.category` is `PROTECT`, so a category with courses cannot be deleted;
  `category_service.delete_category` turns that into a 400 with a usable message
  rather than the 500 an unhandled `ProtectedError` would produce. Creator
  profiles are `SET_NULL`, so they do *not* block deletion and silently lose
  their stated expertise — prefer setting `status` to `INACTIVE` to retire a
  category people have already selected.

Category once lived in `api/courses/`. It was moved with three state-only
migrations plus one table rename (`categories.0001` → `courses.0002` →
`onboarding.0002` → `categories.0002`), so no data was copied or dropped; see
the docstrings on those migrations for why each step avoids touching the
database.

### Course Access

Admins and Approvers have full CRUD over every course, not just their own:
`CourseViewSet` widens its queryset and object permissions for the
administrative tier rather than scoping to `creator`. Two deliberate limits:

- **Submission stays owner-scoped.** Submitting is the author vouching for
  their own work, so an Admin may submit only a course they created.
- **Workflow rules still apply.** Wider access changes *who* may act, not
  *what* the workflow allows — an Admin editing a Published course gets the
  same "Only Draft courses can be edited" error a creator would.

## User Model

The scaffold uses a custom user model at `api.users.models.user.User`.

Key characteristics:

- email is the login identifier
- `username` is removed
- `USERNAME_FIELD = "email"`
- `REQUIRED_FIELDS = ["first_name", "last_name"]`

The model inherits reusable mixins that provide:

- UUID primary keys
- created and updated timestamps
- optional `created_by` and `updated_by` tracking

The custom user manager supports:

- `create_user(email, password=None, **extra_fields)`
- `create_superuser(email, password=None, **extra_fields)`

## Notification Model

The scaffold includes `api.notification.models.Notification` for persisted notifications.

It supports:

- transport type
- title
- content
- content type
- read status
- metadata payloads
- receiver relationship

Implemented helper:

- `emit_in_app_notification(...)`

Extension points intentionally left unimplemented:

- `emit_email_notification(...)`
- `emit_sms_notification(...)`
- `emit_push_notification(...)`

That keeps the scaffold transport-agnostic while still giving projects a clean persistence model for in-app notifications.

## User Activity Log

The scaffold includes `api.users.models.activity_log.UserActivityLog` for reusable audit logging.

It stores:

- user
- actor user
- category
- action
- summary
- details payload
- activity timestamp
- generic target relation
- IP address
- user agent
- request method
- request path

This gives generated projects a standard audit-trail pattern without forcing application-specific logging rules.

## Pagination

The default DRF pagination class is `includes.helpers.PageNumberAPIPagination`.

It returns paginated responses in the scaffold envelope:

```json
{
  "status": true,
  "message": "Successfully retrieved data",
  "data": {
    "paginator": {
      "count": 123,
      "page": 1,
      "page_size": 10,
      "total_pages": 13,
      "next": "http://api.example.org/items/?page=2",
      "next_page_number": 2,
      "previous": null,
      "previous_page_number": null
    },
    "results": []
  }
}
```

The pagination helper also preserves the scaffold behavior where a zero-result search page is treated as valid instead of raising an invalid-page error.

## Error Format

The scaffold uses `drf-standardized-errors` with a custom formatter in `includes.helpers.formatter.TsesExceptionFormatter`.

Error responses follow this shape:

```json
{
  "errors": [
    {
      "type": "validation_error",
      "code": "required",
      "message": "This field is required.",
      "field_name": "email"
    }
  ]
}
```

This keeps error handling consistent across generated projects while still allowing teams to customize the formatter later.

## Middleware and Runtime Defaults

The scaffold middleware stack includes:

- `SecurityMiddleware`
- `WhiteNoiseMiddleware`
- `SessionMiddleware`
- `CorsMiddleware`
- `CommonMiddleware`
- `CsrfViewMiddleware`
- `AuthenticationMiddleware`
- `MessageMiddleware`
- `XFrameOptionsMiddleware`

CORS is installed, but `CORS_ALLOWED_ORIGINS` is empty by default. Teams should populate it deliberately instead of inheriting an unsafe wildcard configuration.

## Celery Bootstrap

Celery is bootstrapped in `config/celery.py` and exposed through `config.__init__` so common Celery commands work with:

```bash
celery -A config worker --loglevel=info
celery -A config beat --loglevel=info
```

A `debug_task` is included as a lightweight smoke-test task for local setup and troubleshooting.

## Storage and S3

The scaffold includes `config/settings/s3.py` as a reserved settings extension point, but it does not yet ship an active S3 storage implementation or required S3 environment variables.

That keeps the base Django scaffold lean. Projects that need file storage can add their preferred storage backend and settings without removing unused defaults.

## GitHub Actions Workflows

The repository includes `.github/workflows/ci.yml` and `.github/workflows/cd.yml`.

**CI** (`ci.yml`) runs on every pull request and push to `staging`/`main`:

- `lint` — `ruff check` (gate) and `ruff format --check` (informational)
- `security` — gitleaks, bandit, pip-audit (all informational until triaged)
- `test` — `manage.py check`, migration drift check, `migrate` from zero, OpenAPI
  schema validation (`drf-spectacular`), and `manage.py test`, against real
  Postgres/Redis service containers
- `docker-build` — builds `.docker/Dockerfile` to verify it still builds (no push)

**CD** (`cd.yml`) runs only after `CI` succeeds on a push (not a PR) to
`staging`/`main`, and only while the `CD_ENABLED` repo variable is `true`:

- checks out the exact commit CI passed on
- builds the Docker image with `.docker/Dockerfile`
- tags it with the short commit SHA and `staging`/`latest`
- pushes it to DigitalOcean Container Registry
- scans it with Trivy (informational until triaged)

Since `workflow_run` only fires from workflow files on the default branch,
`cd.yml` must exist on `main` for CD to run at all — merging this branch into
`main` is required, not optional.

Expected GitHub secrets:

- `DIGITALOCEAN_ACCESS_TOKEN`

Expected GitHub repo variables:

- `DOCR_REGISTRY`
- `DOCR_REPOSITORY`
- `CD_ENABLED` (`false` until the registry/secrets are in place, then `true`)

## Extending the Scaffold

Typical next steps for a generated project:

- add API routes to `config/urls.py`
- create serializers and views in `api/users`, `api/authorization`, and `api/notification`
- implement a concrete authentication flow against `TsesAuthenticationInterface`
- add Celery tasks for email, push, report generation, and scheduled jobs
- wire drf-spectacular schema and docs routes if interactive API docs are needed
- populate `CORS_ALLOWED_ORIGINS` deliberately for the target frontend environments
- add storage settings or a storage service if the project needs file uploads

## Useful Commands

```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver 0.0.0.0:8000
celery -A config worker --loglevel=info
celery -A config beat --loglevel=info --schedule /tmp/celerybeat-schedule
docker compose up --build
docker compose down
```
