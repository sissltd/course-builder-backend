# Generated by Tses 2026-07-09T12:37:38Z
from django.contrib.auth.models import BaseUserManager
from django.utils.translation import gettext_lazy as _


class CustomUserManager(BaseUserManager):
    """User manager for an email-first authentication model.

    The scaffold assumes email is the login identifier. Projects that need phone
    number authentication, usernames, or tenant-aware user creation can extend or
    replace this manager while keeping the rest of the user model intact.
    """

    def create_user(self, email, password=None, **extra_fields):
        """Create and persist a standard user using email as the identity field."""

        if not email:
            raise ValueError(_("The Email field must be set"))

        email = self.normalize_email(email)
        user = self.model(email=email, **extra_fields)
        user.set_password(password)
        user.save(using=self._db)
        return user

    def create_superuser(self, email, password=None, **extra_fields):
        """Create an administrative user with the required permission flags."""

        extra_fields.setdefault("is_staff", True)
        extra_fields.setdefault("is_superuser", True)
        extra_fields.setdefault("is_active", True)

        if extra_fields.get("is_staff") is not True:
            raise ValueError(_("Superuser must have is_staff=True."))
        if extra_fields.get("is_superuser") is not True:
            raise ValueError(_("Superuser must have is_superuser=True."))

        return self.create_user(email, password, **extra_fields)
