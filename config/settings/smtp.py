# Generated by Tses 2026-07-09T12:37:38Z
from decouple import config

EMAIL_PROVIDER = config("EMAIL_PROVIDER", default="smtp")
EMAIL_HOST_USER = config("EMAIL_HOST_USER", default="")
EMAIL_HOST_PASSWORD = config("EMAIL_HOST_PASSWORD", default="")
DEFAULT_FROM_EMAIL = config("DEFAULT_FROM_EMAIL", default="")
# Django's SMTP backend reads EMAIL_TIMEOUT automatically (socket-level
# cap per SMTP operation), so a hung provider cannot stall the whole request
# past Cloudflare's 120s Proxy Read Timeout.
EMAIL_TIMEOUT = config("EMAIL_TIMEOUT", cast=int, default=30)

if EMAIL_HOST_USER:
    EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
    EMAIL_HOST = config("EMAIL_HOST", default="smtp.cloudflare.com")
    EMAIL_PORT = config("EMAIL_PORT", cast=int, default=587)
    EMAIL_USE_TLS = config("EMAIL_USE_TLS", cast=bool, default=True)
    EMAIL_USE_SSL = config("EMAIL_USE_SSL", cast=bool, default=False)
else:
    EMAIL_BACKEND = "django.core.mail.backends.console.EmailBackend"
